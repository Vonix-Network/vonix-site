# ===================================
# VONIX NETWORK - CONFIGURATION
# ===================================
# Copy this file to .env.local and fill in your values
# Most settings are configured via the /setup wizard and stored in the database

# ===================================
# REQUIRED: AUTHENTICATION
# ===================================
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl

# Auth URL - Set to your domain (http://localhost:3000 for dev)
AUTH_URL=http://localhost:3000

# Trust all hosts (required for multiple domains)
AUTH_TRUST_HOST=true

# ===================================
# REQUIRED: DATABASE
# ===================================
# Choose ONE database type and configure accordingly

# DATABASE TYPE (required)
# Options: sqlite, turso, postgres, supabase, mysql, mariadb
DATABASE_TYPE=sqlite

# -----------------------------------
# Option 1: SQLite (Development/Simple)
# -----------------------------------
# Local file-based database, great for development
DATABASE_URL=file:./data/vonix.db

# -----------------------------------
# Option 2: Turso (SQLite in the Cloud)
# -----------------------------------
# Turso is a SQLite-compatible edge database
# DATABASE_TYPE=turso
# DATABASE_URL=libsql://your-database.turso.io
# DATABASE_AUTH_TOKEN=your_turso_auth_token

# -----------------------------------
# Option 3: PostgreSQL
# -----------------------------------
# Standard PostgreSQL database
# DATABASE_TYPE=postgres
# DATABASE_URL=postgresql://user:password@host:5432/database
# DATABASE_SSL=true  # Set to true for SSL connections

# -----------------------------------
# Option 4: Supabase
# -----------------------------------
# Supabase PostgreSQL database
# DATABASE_TYPE=supabase
# DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
# Note: Get connection string from Supabase Dashboard > Settings > Database > Connection String > URI

# -----------------------------------
# Option 5: MySQL
# -----------------------------------
# Standard MySQL database
# DATABASE_TYPE=mysql
# DATABASE_URL=mysql://user:password@host:3306/database

# -----------------------------------
# Option 6: MariaDB
# -----------------------------------
# MariaDB database (MySQL compatible)
# DATABASE_TYPE=mariadb
# DATABASE_URL=mysql://user:password@host:3306/database

# ===================================
# OPTIONAL: CRON JOBS (Production)
# ===================================
# Only needed for Vercel cron authentication
# Generate with: openssl rand -base64 32
# CRON_SECRET=your-cron-secret-here

# ===================================
# OPTIONAL: PUBLIC APP URL
# ===================================
# Only needed if different from AUTH_URL
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===================================
# NOTE: All other settings configured in /setup wizard
# ===================================
# - Admin account
# - Site name & description
# - Stripe payment keys (optional)
# - Square payment keys (optional)
# - Email settings (optional)
# - Discord integration (optional)
# - And more...
